<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #333;
            color: #fff;
            margin: 0;
            padding: 20px;
        }

        h1 {
            color: #ffa500;
        }

        form {
            display: flex;
            flex-direction: column;
            max-width: 400px;
        }

        label {
            margin-top: 10px;
            color: #ffa500;
        }

        input, button {
            margin-top: 10px;
            padding: 8px 12px;
            border-radius: 5px;
        }

        button {
            background-color: #ffa500;
            color: #333;
            border: none;
            cursor: pointer;
        }
    </style>
    <title>Edit Product</title>
</head>

<body>
    <h1>Edit Product</h1>

    <form action="/admin/update-product/<%= findProduct._id %>" method="post" enctype="multipart/form-data">
        <label for="productName">Product Name:</label>
        <input type="text" id="productName" name="productName" value="<%= findProduct.productName %>" required>

        <label for="productPrice">Product Price:</label>
        <input type="text" id="productPrice" name="productPrice" value="<%= findProduct.productPrice %>" required>

        <label for="productImage">Product Image URL:</label>
        <input type="file" id="productImage" name="productImage" accept="image/*" required>

        <button type="submit">Update Product</button>
    </form>

    <a href="/admin/adminHome">Back to Admin Page</a>
</body>

</html>

